@model Theater.PerformanceAuthor

@{
    ViewData["Title"] = "Adding PrAu";
}

<h4>Додати автора</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddAuthor">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PrId" class="control-label"></label>
                <select asp-for="PrId" class="form-control" asp-items="ViewBag.PrId"></select>
            </div>
            <div class="form-group">
                <label asp-for="AuId" class="control-label"></label>
                <select asp-for="AuId" class="form-control" asp-items="ViewBag.AuId"></select>
            </div>
            <div class="form-group">
                <label asp-for="Info" class="control-label"></label>
                <input asp-for="Info" class="form-control" />
                <span asp-validation-for="Info" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Додати" class="btn btn-primary" /> |
                <a asp-action="Author" asp-route-id="@ViewBag.Back" class="btn btn-info">Повернутись до списку</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
