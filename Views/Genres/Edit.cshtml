@model Theater.Genres

@{
    ViewData["Title"] = "Edit";
}

<h1>Змінити</h1>

<h4>Жанр</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="GnId" />
            <div class="form-group">
                <label asp-for="GnName" class="control-label"></label>
                <input asp-for="GnName" class="form-control" />
                <span asp-validation-for="GnName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Зберегти" class="btn btn-success" /> |
                <a asp-action="Index" class="btn btn-info">Повернутись до списку</a>

            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
